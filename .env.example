# SMTP-Zugangsdaten (z.B. Gmail, Strato, GMX, eigener Server)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=ihre@email.at
SMTP_PASS=ihr-passwort

# Wohin sollen die Anfragen gesendet werden?
RECIPIENT_EMAIL=florian.langer@gruppe19.at

# Port des Servers (optional, Standard: 3000)
PORT=3000
